"""Domain model: entities (the IR), signature clustering, waveform assembly.

Pure Python plus ``lark``-produced inputs.  Must NEVER import ``tkinter``
(§2.3 import firewall).
"""

from __future__ import annotations
